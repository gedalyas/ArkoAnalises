# URL da API (backend na Railway). Sem barra no final.
# Em desenvolvimento NÃO precisa definir — o Vite faz proxy de /api → :3333.
# Em produção (Vercel), defina com a URL pública da API:
# VITE_API_URL="https://sua-api.up.railway.app"
VITE_API_URL=""
